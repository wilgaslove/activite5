name: careconnect
description: CareConnect - application mobile de santé communautaire (scaffold).
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^2.15.0
  cloud_firestore: ^4.9.0
  firebase_auth: ^4.6.0
  firebase_messaging: ^14.4.0
  provider: ^6.0.5
  google_maps_flutter: ^2.4.0
  geolocator: ^9.0.2
  image_picker: ^0.8.7+5
  sqflite: ^2.2.8+4
  flutter_local_notifications: ^12.0.4
  lottie: ^2.3.0
  flutter_animate: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/lottie/